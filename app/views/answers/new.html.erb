Question: <%= @answer.m_word.question_word %><br>
<%= form_for [@question.becomes(Question), @answer] do |f| %>
<%= f.text_field :characters %>
<%= f.hidden_field :m_word_id %>
<%= f.submit %>
<% end %>
